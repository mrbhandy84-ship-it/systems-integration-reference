# Retry Strategy at Integration Boundaries

This document covers retry configuration for outbound calls to external systems. Retry behavior is defined per error class and per integration target. Dead-letter thresholds and budget limits apply at the boundary level.
