# Adapter Pattern

This pattern isolates external system integration logic behind a stable internal interface. The adapter handles protocol translation, authentication, and error mapping for its target system. Internal consumers interact with the adapter interface only.
